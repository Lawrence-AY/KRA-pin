# Project Instructions

- This is a CommonJS Express.js service for KRA API integrations.
- Keep KRA credentials and generated access tokens in environment variables or runtime memory only.
- Use bcryptjs for local client password verification.
- Keep KRA HTTP calls inside `src/services/kraClient.js`.
- Run `npm install` and `node --check` on changed JavaScript files before finishing.
